type AttendanceStatistics {
    fishingGround: FishingGround!
    totalNumberOfVisits: Int!
    totalAmount: Int!
    totalWeight: Float!
}

extend type Query {
    allAttendanceStatistics: [AttendanceStatistics!]
}